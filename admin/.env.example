# Absolute base URL of the Calen API for production builds.
# In dev this is unset and requests are proxied to http://localhost:3001 (see
# vite.config.js). The API server must allowlist this app's origin via
# CORS_ORIGINS.
VITE_API_BASE_URL=
