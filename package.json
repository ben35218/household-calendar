{
  "name": "household-calendar",
  "version": "1.0.0",
  "description": "Household Calendar - home inventory, manuals & maintenance scheduler",
  "private": true,
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev": "npm run dev:server",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "@types/react": "~19.2.14",
    "concurrently": "^8.2.2",
    "typescript": "~6.0.3"
  },
  "dependencies": {
    "expo": "^56.0.12"
  }
}
