{
  "name": "household-calendar",
  "version": "1.0.0",
  "description": "Calen - end-to-end-encrypted household calendar & AI assistant",
  "private": true,
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev": "npm run dev:server",
    "start": "npm --prefix server start",
    "test": "npm run test:server && npm run test:shared && npm run test:mobile",
    "test:server": "npm --prefix server test",
    "test:shared": "npm --prefix shared/crypto test && npm --prefix shared/calendar test && npm --prefix shared/weather test",
    "test:mobile": "npm --prefix mobile test"
  },
  "devDependencies": {
    "@types/react": "~19.2.14",
    "concurrently": "^8.2.2",
    "typescript": "~6.0.3"
  },
  "dependencies": {
    "expo": "^56.0.12",
    "react": "19.2.3",
    "react-native": "0.85.3"
  }
}
